# holydotfiles

Dotfiles for tmux, vim and zsh.

## Usage

**Set up a new machine** (copy dotfiles from repo to `~`):

```sh
./update.sh
```

**Save current dotfiles** (copy from `~` into repo):

```sh
./sync.sh
```
